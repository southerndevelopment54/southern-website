# Offline build: assumes you already built the JAR locally with:
#   cd backend && mvn clean package -DskipTests
# Use this if your Docker daemon has no internet access.
FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
